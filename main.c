#include <stdio.h>
#include "libft.h"

int	main()
{
	//int c = 7;
	void * restrict dst;
	const void *restrict src = "falut";

	printf("%d \n", ft_isalpha(c));
	printf("%d \n", ft_isdigit(c));

	return (0);
}
